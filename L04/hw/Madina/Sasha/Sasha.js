const player = {
  nick: "CUBE",
  points: 0,
  startGame() {
    const userValue = confirm ("Играем?");
    const yesOrNo = Math.random() > 0.0;
    if (userValue == yesOrNo) {
      alert("Делайте один шаг вперед");
      else if 
    //   if ((yesOrNo = Math.random() > 0.166)) {
    //     alert("Делайте два шага вперед");
    //   }
    // }
    // Math.random() > 0.166 ||
    //   Math.random() > 0.332 ||
    //   Math.random() > 0.498 ||
    //   Math.random() > 0.664 ||
    //   Math.random() > 0.83;
  },
};
